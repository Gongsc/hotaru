{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chart",
  "description": "Chart popover: needs window mutations (hide/drag) that core:default does not grant. Sizing and repositioning run in Rust so the popover can stay anchored to the tray icon.",
  "windows": ["chart"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-start-dragging"
  ]
}
