{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the local settings and chart windows. The main window loads the remote Komari dashboard and intentionally has no IPC access.",
  "windows": ["settings", "chart"],
  "permissions": ["core:default"]
}
